spring:
  application:
    name: ms-finances
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/meu_banco
    username: root
    password: root